invito_ninja_giorgio:
  group: ninja
  user: giorgio
  url_string: <%= SecureRandom.urlsafe_base64 %>
  expiration_date: <%= DateTime.now + 1.day %>
  created_at: <%= Time.now.to_s %>
  updated_at: <%= Time.now.to_s %>

invito_ninja_giovanni:
  group: ninja
  user: giovanni
  url_string: <%= SecureRandom.urlsafe_base64 %>
  expiration_date: null
  created_at: <%= Time.now.to_s %>
  updated_at: <%= Time.now.to_s %>

invito_pirati_mario_scaduto:
  group: pirati
  user: mario
  url_string: <%= SecureRandom.urlsafe_base64 %>
  expiration_date: <%= DateTime.now - 1.day %>
  created_at: <%= Time.now.to_s %>
  updated_at: <%= Time.now.to_s %>

invito_pirati:
  group: pirati
  user: null
  url_string: <%= SecureRandom.urlsafe_base64 %>
  expiration_date: null
  created_at: <%= Time.now.to_s %>
  updated_at: <%= Time.now.to_s %>

invito_ninja_scaduto:
  group: ninja
  user: null
  url_string: <%= SecureRandom.urlsafe_base64 %>
  expiration_date: <%= DateTime.now - 1.day %>
  created_at: <%= Time.now.to_s %>
  updated_at: <%= Time.now.to_s %>